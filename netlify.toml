[build]
  base = "ubuntu/command-x-frontend"
  publish = "dist"
  command = "npm install -g pnpm && npm pkg set dependencies.jwt-decode=\"^4.0.0\" && npm pkg set devDependencies.terser=\"^5.29.2\" && pnpm install --no-frozen-lockfile && pnpm run build:no-typecheck"

[build.environment]
  NODE_VERSION = "18"

# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Force HTTPS
[[redirects]]
  from = "http://*"
  to = "https://:splat"
  status = 301
  force = true

# Handle asset paths
[[headers]]
  for = "/*"
    [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
